---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: peers.clusterlink.net
spec:
  group: clusterlink.net
  names:
    kind: Peer
    listKind: PeerList
    plural: peers
    singular: peer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Peer represents a location (or site) that can be used to import
          services from.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec represents the peer attributes.
            properties:
              gateways:
                description: Gateways serving the Peer.
                items:
                  description: Endpoint represents a network endpoint (i.e., host
                    or IP and a port).
                  properties:
                    host:
                      description: Host or IP address of the endpoint.
                      type: string
                    port:
                      description: Port of the endpoint.
                      type: integer
                  required:
                  - host
                  - port
                  type: object
                type: array
            required:
            - gateways
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
