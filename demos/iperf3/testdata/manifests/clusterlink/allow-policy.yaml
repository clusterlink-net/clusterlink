apiVersion: clusterlink.net/v1alpha1
kind: AccessPolicy
metadata:
  name: iperf3-policy-allow
  namespace: clusterlink-system
spec:
  action: allow
  privileged: false
  from:
    - workloadSelector: {}
  to:
    - workloadSelector: {}
